PROJECT = grpc_lib
PROJECT_DESCRIPTION = Some functions shared between grpc and grpc_client
PROJECT_VERSION = 0.1.0

# Whitespace to be used when creating files from templates.
SP = 4

BUILD_DEPS = gpb

include erlang.mk
